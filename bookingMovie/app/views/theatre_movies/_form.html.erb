<%= form_for(@theatre_movie) do |f| %>
  <% if @theatre_movie.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@theatre_movie.errors.count, "error") %> prohibited this theatre_movie from being saved:</h2>

      <ul>
      <% @theatre_movie.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :theatre_id %><br>
    <%= f.number_field :theatre_id %>
  </div>
  <div class="field">
    <%= f.label :movie_id %><br>
    <%= f.number_field :movie_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
